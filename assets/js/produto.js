document.addEventListener("section:loaded", (e) => {
  // caso a página de produto seja carregada dinamicamente, aqui podemos ler query params
  // e popular os dados do produto via fetch (API) ou dataset.
});
